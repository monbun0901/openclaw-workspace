# SOP: Development Environment Setup

## Purpose
Describe how to bootstrap the dev environment for new tasks.

## Steps
1. Clone the relevant repo.
2. Install dependencies via `npm install` / `pip install` as needed.
3. Configure environment variables listed in `.env.example`.
4. Run smoke tests or `npm test`.

## Verification
- Developer can run local server without errors.
